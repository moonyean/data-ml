# ── 환경변수 (절대 올리면 안 됨) ─────────────────────────────
.env
.env.*
!.env.example

# ── Python ────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python

.claude

# 가상환경
venv/
.venv/
env/
.env/

# 학습된 모델 파일 (용량 크고 재현 가능)
*.joblib
*.h5
*.pt
*.pth

# 로그
*.log
uvicorn.log

# ── Jupyter Notebook ──────────────────────────────────────────
.ipynb_checkpoints/
# 노트북 자체는 올릴 수 있지만 출력 결과만 제외하려면 아래 주석 해제
# *.ipynb

# ── 데이터셋 (용량 크면 제외, 공개 데이터라 올려도 무방하면 주석 처리) ──
# *.csv

# ── Node.js / 프론트엔드 ──────────────────────────────────────
FrontEnd/node_modules/
FrontEnd/.pnpm-store/
FrontEnd/dist/
FrontEnd/build/
FrontEnd/.cache/
FrontEnd/.vite/

# pnpm
pnpm-lock.yaml

# ── OS / 에디터 ───────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo
